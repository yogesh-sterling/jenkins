node {
    
    stage('Clone sources') {
        echo 'clone...'
    }

    stage('Artifactory configuration') {
        echo 'Artifactory'       
    }
    
    stage('Maven build') {
        echo 'Maven'
    }

    stage('Publish build info') {
        echo 'Deploy'
    }
}
